SELECT NAME, COUNT(NAME) AS 'COUNT'
FROM ANIMAL_INS 
GROUP BY NAME
HAVING COUNT(NAME)>1
ORDER BY NAME

/*
--오답

SELECT NAME, COUNT(NAME) AS 'COUNT'
FROM ANIMAL_INS 
WHERE COUNT(NAME)>1   --WHERE 아님!!!!
GROUP BY NAME
ORDER BY NAME
*/
